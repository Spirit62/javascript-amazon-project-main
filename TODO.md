# TODO

- [x] Fix `data/cart.js` (broken localStorage access, export/import, and remove/add logic)
- [x] Fix `scripts/checkout.js` (invalid HTML attribute for delete link, wrong selector for delete links, and dataset usage)

- [ ] Open `index.html` and verify products render and cart quantity updates
- [ ] Open `checkout.html` and verify cart items render and Delete works without JS errors

